# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Predictor year — update this each academic year without redeploying.
NEXT_PUBLIC_PREDICTOR_YEAR=2025

# Optional: centralized client-side error tracking webhook
# Example: an incident tool or webhook receiver endpoint
NEXT_PUBLIC_CLIENT_ERROR_WEBHOOK_URL=
